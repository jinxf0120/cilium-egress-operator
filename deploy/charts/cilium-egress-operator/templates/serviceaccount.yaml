apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cilium-egress-operator.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cilium-egress-operator.labels" . | nindent 4 }}
